/**
 * Stub for `@banavasi/adms-rds-ui-icons/setup`.
 *
 * In a Vite project the `rdsIcons()` plugin replaces this with a generated
 * module that registers your configured icons. Without the plugin this file
 * is a no-op — icons will not render until setup runs.
 */
export {}
