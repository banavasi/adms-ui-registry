{
  "name": "adms-rds-ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "version": "pnpm changeset version",
    "release": "pnpm --filter @adms-rds-ui/cli publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.2",
    "@changesets/cli": "^2.29.8",
    "turbo": "^2.3.0"
  }
}
